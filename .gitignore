node_modules/
dist/
coverage/

.impeccable/build/
.impeccable/review/
.impeccable/mocks/
.impeccable/config.local.json

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
